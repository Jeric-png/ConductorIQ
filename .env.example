OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4.1-mini
OPENAI_IMAGE_MODEL=gpt-image-1.5

# Local-only OpenAI key read by the Vite dev-server proxy.
# Do not use VITE_ for private keys because VITE_ variables are exposed to the browser bundle.
# Keep .env.local private and never commit real secrets.
